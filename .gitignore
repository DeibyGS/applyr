__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.db
.env
.DS_Store
.claude/
HANDOFF.md
# specs/ is versioned: design docs belong with the code they describe, the same
# way docs/adr/ does. HANDOFF.md stays out — that one is session state, not design.

# Coverage artifacts
.coverage
.coverage.*
htmlcov/

# Personal data — never commit (public repo)
cv/
*.pdf

# Agent instructions written by `applyr setup-agent` when it runs inside this repo.
# They are generated copies of templates/AGENT_INSTRUCTIONS.md, not sources.
CLAUDE.md
.cursorrules
.worktrees/
