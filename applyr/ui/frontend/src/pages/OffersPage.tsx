import { Briefcase } from "lucide-react";
import { ComingSoon } from "@/layout/ComingSoon";

export default function OffersPage() {
  return (
    <ComingSoon
      title="Offers"
      message="A dedicated offer queue view is coming in a future update."
      icon={Briefcase}
    />
  );
}
